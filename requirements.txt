# Requirements for Dynamic Island GNOME
# Most packages are pre-installed by default on Ubuntu Desktop

# Core GUI & Rendering (System Packages)
# sudo apt install python3-gi python3-gi-cairo python3-cairo gir1.2-gtk-3.0

# D-Bus & System Metrics
# sudo apt install python3-dbus python3-psutil

# Audio backend: PipeWire / WirePlumber (wpctl) - default on Ubuntu 24.04+
# Power backend: UPower - default on Ubuntu
